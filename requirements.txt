# Core dependencies
pyyaml>=6.0
python-dotenv>=1.0.0

# Docker support for containerized environments
docker>=7.0.0

# HTTP client for communication with containers
httpx>=0.27.0

# Async support for nested event loops
nest-asyncio>=1.5.0

# Note: rayfine must be installed separately from crates/rayfine-python for remote execution
# cd ../crates/rayfine-python && maturin develop --release